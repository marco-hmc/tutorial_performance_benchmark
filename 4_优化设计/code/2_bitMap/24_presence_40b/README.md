题目：判断一个数是否在 40 亿个未排序的不重复 unsigned int 整数中

两种主要思路：

- 位图（Bitmap）：对 2^32 个可能值用 1 bit 表示是否存在，约需 512MB 内存；在示例中使用小范围模拟。
- 逐位分治（按二进制位划分）：按最高位将数据分为两组，选择与待查数最高位相同的组继续划分，直到找到或排除。示例用小规模数据演示该思路。

本目录包含 `presence_40b.cpp` 演示两种方法（小范围），`sample_presence.txt` 样例输入，以及 `run_test.sh` 用于编译并运行演示。
